namespace CSharp72Demos
{
    class PrivateProtectedDemo
    {
        public static void Show()
        {
            //private protected from C# -> 
            /*
             * Access is limited to the containing class or types derived 
             * from the 
             * containing class within the current assembly. */
        }
    }
}
